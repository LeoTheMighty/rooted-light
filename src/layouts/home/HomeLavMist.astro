---
// Lavender mist: the quietest refinement — small-caps header row,
// one centered narrow column, hairline section tops. Nothing else.
import MockupShell from "./MockupShell.astro";
import layoutCss from "./HomeLavMist.css?raw";
import { home, nav } from "./_content";

interface Props {
  packCss: string;
  title: string;
  blurb?: string;
}
const { packCss, title, blurb } = Astro.props;

const sections = [
  { id: "offerings", title: "Offerings", items: home.offerings },
  { id: "modalities", title: "Modalities", items: home.modalities },
  { id: "about", title: "About", items: home.about },
];
---

<MockupShell packCss={packCss} layoutCss={layoutCss} title={title} blurb={blurb}>
  <header class="row">
    <span class="brand">{home.name}</span>
    <nav aria-label="Main">
      <ul>
        {nav.map((n) => <li><a href={n.anchor}>{n.label}</a></li>)}
      </ul>
    </nav>
  </header>

  <main>
    <section class="hero" id="welcome">
      <h1>{home.tagline}</h1>
      <p class="intention">{home.intention}</p>
      <a class="cta" href={home.bookingUrl}>Book a session</a>
    </section>

    {
      sections.map((s) => (
        <section class="strip" id={s.id}>
          <h2>{s.title}</h2>
          {s.items.map((item) => (
            <article>
              <h3>{item.title}</h3>
              <p>{item.blurb}</p>
            </article>
          ))}
        </section>
      ))
    }

    <section class="strip" id="resources">
      <h2>Resources</h2>
      <p>{home.resources.map((r) => r.title).join(" · ")}</p>
      <p class="words">{home.intentionWords.join("  ·  ")}</p>
    </section>
  </main>

  <footer class="foot">
    <p>{home.legalName} · {home.tagline}</p>
  </footer>
</MockupShell>
