# devx deploy — empty stub. Generated by /devx-init.
# Wire your deploy steps inside the markers; re-running /devx-init preserves
# them.
# >>> devx
name: devx-deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    steps:
      - run: echo "[devx] deploy stub — replace with your project's deploy steps"
# <<< devx
