# devx promotion — placeholder for Phase 1 (`/devx-manage` promote develop → main).
# This file is a stub. The real promotion workflow lands when the develop/main
# split is wired end-to-end. Generated by /devx-init.
# >>> devx
name: devx-promotion

on:
  workflow_dispatch:
  schedule:
    # Disabled by default — uncomment once `/devx-manage` runs the gate logic.
    # - cron: "0 */6 * * *"
    - cron: "0 0 31 2 *"  # never (Feb 31 is a no-op cron entry)

permissions:
  contents: read
  pull-requests: write

jobs:
  promote:
    name: promote
    runs-on: ubuntu-latest
    steps:
      - run: echo "[devx] promotion placeholder — wired in Phase 1 by /devx-manage"
# <<< devx
