# devx CI — empty stack (no source files yet)
# Generated by /devx-init. Replace these echo-only no-op gates once your
# stack is in place — re-running /devx-init upgrades the workflow when a
# stack is detected.
# >>> devx
name: devx-ci

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

permissions:
  contents: read

concurrency:
  group: devx-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "[devx] empty-stack lint placeholder — no-op until a stack is detected"

  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "[devx] empty-stack test placeholder — no-op until a stack is detected"

  coverage:
    name: coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "[devx] empty-stack coverage placeholder — no-op until a stack is detected"
# <<< devx
